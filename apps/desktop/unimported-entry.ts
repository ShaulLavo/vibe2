/**
 * Placeholder entry for the `unimported` tooling.
 *
 * The desktop shell is authored in Rust (src-tauri/), so this file exists only
 * so unimported has a TypeScript entry point to walk. No runtime code should
 * be imported here.
 */
export {}
